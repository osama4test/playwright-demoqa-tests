{
  "name": "playwright-qa-automation-boilerplate",
  "version": "1.0.0",
  "scripts": {
    "test": "npx playwright test",
    "report": "npx allure generate ./allure-results --clean -o ./allure-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.39.0",
    "@types/node": "^24.0.14",
    "allure-playwright": "^2.13.0",
    "dotenv": "^16.0.0",
    "eslint": "^8.0.0",
    "prettier": "^2.0.0",
    "typescript": "^4.0.0"
  }
}
